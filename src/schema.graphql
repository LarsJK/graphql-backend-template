type Query {
  users: [User]!
  user(userId: ID!): User
}

type Mutation {
  createUser(name: String!): User
  deleteUser(id: ID!): User
}

type Subscription {
  addedUser: User!
  deletedUser: User!
}

type User {
  id: ID!
  name: String!
}
