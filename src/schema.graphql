type Query {
  users: [User]!
  user(userId: ID!): User
  workspaces: [Workspace!]!
}

type Mutation {
  createUser(name: String!): User
  deleteUser(id: ID!): User
}

type User {
  id: ID!
  name: String!
}

type Workspace {
  id: ID!
  title: String!
  workers: [User!]!
}
